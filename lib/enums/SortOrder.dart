/// An enumeration that represents the order in which to sort a list of objects.
///
/// This enumeration has two values: [asc] for ascending order, and [desc] for descending order.
enum SortOrder {
  asc,
  desc
}
